# Knearby Website

# Getting Started

## Development

To start development, install dependencies:

```sh
npm install;
```

Then start the application:

```sh
npm start;
```

To build the production files:

```sh
npm run build;
```

To deploy the site:

```sh
npm run deploy;
```
